{
  "name": "book-buddy-plus",
  "version": "1.0.0",
  "type": "module",
  "description": "",
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=22"
  },
  "main": "index.js",
  "imports": {
    "#*": "./*.js"
  },
  "keywords": [],
  "scripts": {
    "db:reset": "npm run db:schema && npm run db:seed --prefix backend",
    "db:schema": "psql -d book_buddy -f db/schema.sql --prefix backend",
    "db:seed": "node --env-file=../.env db/seed.js --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend --prefix backend"
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "prettier-plugin-embed": "^0.5.0",
    "prettier-plugin-organize-imports": "^4.2.0",
    "prettier-plugin-pkg": "^0.21.2",
    "prettier-plugin-sql": "^0.19.2"
  }
}
